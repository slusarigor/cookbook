addons:
  postgresql: 10.3
before_script:
  - psql -c 'create database cookbook_test;' -U postgres
dist: trusty
language: ruby
rvm:
  - 2.5.0
script:
  - bundle exec rails db:migrate RAILS_ENV=test
  - bundle exec rspec
  - bundle exec rubocop